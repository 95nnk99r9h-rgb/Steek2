/** ZENTRALE KONTAKT-EINSTELLUNGEN – keine Passwörter oder API-Schlüssel eintragen.
 * Anleitung: README.md. Änderungen der Service-Worker-Version in sw.js mitziehen.
 */
window.SITE_CONFIG = Object.freeze({
  contactEmail: "", // Echte Empfängeradresse, z. B. die von Marcel bereitgestellte Adresse.
  formEndpoint: "" // Optional: öffentlicher HTTPS-Formular-Endpunkt (Formspree-kompatibel).
});
